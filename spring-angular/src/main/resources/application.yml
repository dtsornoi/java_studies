spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tutorials_db?allowPublicKeyRetrieval=true&useSSL=false
    username: springstudent
    password: springstudent
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update
